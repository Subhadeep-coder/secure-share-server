{
    "version": 2,
    "builds": [
        {
            "src": "src/main.rs",
            "use": "@vercel/static-build",
            "config": {
                "rootDirectory": "/",
                "buildCommand": "cargo build --release",
                "outputDirectory": "target/release"
            }
        }
    ]
}